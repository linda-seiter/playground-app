//Declare constant "width" and assign it an integer value 9.
const width = 10;

//Declare constant "height" and assign it an integer value 2.
const height = 2;

//Declare constant "area" and assign it to the product of width * height.
const area = width * height;

//Use the console.log() function to print the values stored in each variable.

//console.log(`width: ${width}`);
//console.log(`height: ${height}`);
//console.log(`area: ${area}`);
console.log(`The area of a ${width}x${height} rectangle is ${area}.`);

const city = document.getElementById("city");
city.style.background = "pink";
